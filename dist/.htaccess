<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Serve index.html for all non-file, non-directory routes
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^.*$ /index.html [L,QSA]
</IfModule>
